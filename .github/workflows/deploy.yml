name: Deploy Web Gamepadecho.echo on:echo   push:echo     branches: [ "main" ]echo     paths:echo       - 'WebGamepad/**'echo.echo permissions:echo   contents: readecho   pages: writeecho   id-token: writeecho.echo concurrency:echo   group: "pages"echo   cancel-in-progress: trueecho.echo jobs:echo   deploy:echo     environment:echo       name: github-pagesecho       url: ${{ steps.deployment.outputs.page_url }}echo     runs-on: ubuntu-latestecho     steps:echo       - name: Checkoutecho         uses: actions/checkout@v4echo.echo       - name: Setup Pagesecho         uses: actions/configure-pages@v4echo.echo       - name: Upload artifactecho         uses: actions/upload-pages-artifact@v3echo         with:echo           path: 'WebGamepad'echo.echo       - name: Deploy to GitHub Pagesecho         id: deploymentecho         uses: actions/deploy-pages@v4
